#tuples can store all python datatypes 
#tuples are immutable meaning they cant be changed

my_tuple = (1, 'strings', 4.5, True)
#OR: my_tuple = 1, 'strings', 4.5, True

for x in my_tuple: #prints all values of tuples
    print(x)